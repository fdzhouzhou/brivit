"""Test-suite configuration.

Markers themselves are declared in pyproject.toml [tool.pytest.ini_options];
this file only exists to anchor pytest's rootdir detection when the package
has been installed in editable mode.
"""
